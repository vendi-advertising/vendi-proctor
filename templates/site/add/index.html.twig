{% extends 'base-with-nav.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    <style>
    form > div {
        margin: 0 auto;
        max-width: 50rem;
    }

    form > div > div {
        display: grid;
        grid-column-gap: 1rem;
        grid-template-columns: 1fr 3fr;
    }

    form > div > div > label {
        text-align: right;
    }
    </style>

{% endblock %}

{% block title %}Add New Site{% endblock %}

{% block main_body %}

{{ form_start(websiteForm) }}
{{ form_widget(websiteForm) }}
<div>
    <div>
        <div></div>
        <button class="btn btn-primary" type="submit">Create!</button>
    </div>
</div>
{{ form_end(websiteForm) }}

{% endblock %}
